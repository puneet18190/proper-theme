Hi <%= @user.username %>
<br/>
Your searching results are:
<br/>
<% @properties.each do |task| %>
  Property name : <%= task.name %>,
  Bath : <%= task.bath  %>,
  Beds : <%= task.beds %> &
  Price :<%= task.price %>
  <%= link_to "Click on the link to view the details", "http://#{@request}/properties_detail/#{task.id}" %><br/>
<% end %>